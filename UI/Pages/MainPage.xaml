<Page x:Class="UI.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:UI"
      mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="5*" />
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>
        
        <ListView x:Name="RequestsListView"
                  Grid.Column="0"
                  Margin="5"
                  SelectionMode="Single">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="ID" Width="50" DisplayMemberBinding="{Binding Id}" />
                    <GridViewColumn Header="Дата" Width="100" DisplayMemberBinding="{Binding Date}" />
                    <GridViewColumn Header="Клиент" Width="200" DisplayMemberBinding="{Binding ClientFullName}" />
                    <GridViewColumn Header="Телефон" Width="150" DisplayMemberBinding="{Binding ClientPhoneNumber}" />
                    <GridViewColumn Header="Испонитель" Width="150" DisplayMemberBinding="{Binding PerformerFullName}" />
                    <GridViewColumn Header="Статус" Width="100" DisplayMemberBinding="{Binding Status}" />
                </GridView>
            </ListView.View>
        </ListView>
        
        <DockPanel Grid.Column="1"
                   Margin="5">
            <StackPanel DockPanel.Dock="Top">
                <Button x:Name="AddButton"
                        Margin="5"
                        Content="Добавить"/>

                <Button x:Name="EditButton"
                        Margin="5"
                        Content="Редактировать"/>

                <Button x:Name="DeleteButton"
                        Margin="5"
                        Content="Удалить"/>
            </StackPanel>
        </DockPanel>
    </Grid>
</Page>
